#!/bin/bash
export PASSWORD="userpass"
export HOSTNAME="$HOSTNAME"

if [ "$1" = "-d" ] ; then
  export TCPCHAT_DEBUG=Y
  shift
fi

if [ "$1" != "" ] ; then
  echo "On port $1"
  port="$1"
else
  echo "On stdio"
  port="-"
fi

exec tcpchat -p $port -f chats.xml

