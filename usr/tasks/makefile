include $(if $(MAKFILES_DIR), $(MAKFILES_DIR), $(HOME)/Makefiles)/common.mk
include $(HOME)/Makefiles/units.mk

ADAVIEW := $(REPOSIT)
ADACTL := true

EXES := $(UNITS)

CLIBS_accountants := $(UTILS_SLIBS)
CLIBS_servprio := $(UTILS_SLIBS)

include $(TEMPLATES)/ada.mk

