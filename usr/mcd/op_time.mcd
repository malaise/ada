# Operation between 2 times

# Syntax error message
[
"Error" putl
ssize popn retall
] E popr

# Check 1 arg
ssize 1 /= E pushr ifcall

# Make result milliseconds
3600.0 * 1000.0 * round 

# Fix negative values
dup 0 < [ "-" put absv ] ifcall 

# Convert to " days-hh:mm:ss.mmm" and remove days
daysof 7 18 strsub
# Replace : by h, : by m and put
"h" 3 swap3 strrep
"m" 6 swap3 strrep
"s" 9 swap3 strrep
putl

