# Operation between 2 times

# Syntax error message
[
"Error" putl
ssize popn retall
] E popr

# Check 1 arg
ssize 1 /= E pushr ifcall

# Make result milliseconds
3600.0 * 1000.0 * round

# Fix negative values
dup isneg [ "-" put absv ] ifcall

# Check for days
daysof dup 1 5 strsub strinte dup
isnul [ pop ] [ 1 format put "j" put ] ifte call
# Convert to " days-hh:mm:ss.mmm" and remove days
7 18 strsub
# Replace : by h, : by m and put
3 "h" strrep
6 "m" strrep
9 "s" strrep
putl

