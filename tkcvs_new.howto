To add a new dir:
- be on top of dir, select it, "Add the selected files to the repository"
- go in dir, select files, "Add the selected files to the repository"
- select file again, "Check the selected files in to the repository"
*** leave version number and comment empty ***
done it
